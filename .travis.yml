language: cpp
compiler:
  - gcc
matrix:
  include:
    - os: linux
      addons:
        apt:
          sources:
            - ubuntu-toolchain-r-test
          packages:
            - g++-7
      env:
        - MATRIX_EVAL="CC=gcc-7 && CXX=g++-7"

before_install:
  - sudo apt-get -qq update
  - sudo apt-get install -y libxml2-dev libboost-all-dev libllvm3.9v4
  - eval "${MATRIX_EVAL}"

script:
    - exec mkdir build
    - gcc -v
    - g++ -v
    #- bundle exec cd build && bundle exec cmake .. && bundle exec make

